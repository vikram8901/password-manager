# password-manager
The current version stores passwords in plain text inside the JSON file. Fine for learning, not ideal for real-world security. A real password manager should encrypt stored passwords.
